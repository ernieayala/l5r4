<nav class="sheet-tabs tabs" data-group="primary">
  {{#each tabs as |tab|}}
    <a class="item {{tab.cssClass}}" data-action="changeTab" data-tab="{{tab.id}}">
      {{tab.label}}
    </a>
  {{/each}}
</nav>
